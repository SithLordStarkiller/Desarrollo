@{
    ViewBag.Title = "Consulta Factor Actividad Económica";
}

@section Styles{
    <link href="~/Content/jquery-ui.css" rel="stylesheet" />
}

@*<ol class="breadcrumb">
        <li><span class="glyphicon glyphicon-home" aria-hidden="true"></span></li>
        <li><a href="#">Catalogos</a></li>
        <li><a href="~/Catalogo/FactoresActividadEconomica" class="active">Factor Actividad Económica</a></li>
    </ol>*@
<div class="flip">
    <div class="front">
        <div class="top-buffer">
            <h3>@ViewBag.Title</h3>
            <hr class="red">
        </div>
        @*<div class="col-md-3 form-group">
            <label class="control-label">Estatus:</label>
            <div class="col-md-12">
                <label class="radio-inline" for="activo">
                    <input type="radio" name="estatus" value="true" checked="checked">Activo
                </label>
                <label class="radio-inline" for="inactivo">
                    <input type="radio" name="estatus" value="false">Inactivo
                </label>
            </div>
        </div>*@
        <div class="ui-widget col-md-3 form-group">
            <label class="control-label">Clasificación:</label>
            @Html.DropDownList("Clasificaciones", (IEnumerable<SelectListItem>)ViewBag.Clasificaciones, "Seleccione", new { @class = "form-control" })
        </div>
        <div class="ui-widget col-md-3 form-group">
            <label class="control-label">Factor:</label>
            @Html.DropDownList("Factores", (IEnumerable<SelectListItem>)ViewBag.Factores, "Seleccione", new { @class = "form-control" })
        </div>
        <div class="ui-widget col-md-3 form-group">
            <label class="control-label">División:</label>
            @Html.DropDownList("Divisiones", (IEnumerable<SelectListItem>)ViewBag.Divisiones, "Seleccione", new { @class = "form-control" })
        </div>
        <div class="ui-widget col-md-3 form-group">
            <label class="control-label">Grupo:</label>
            @Html.DropDownList("Grupos", (IEnumerable<SelectListItem>)ViewBag.Grupos, "Seleccione", new { @class = "form-control" })
        </div>
        <div class="pull-right">
            <button type="button" class="btn btn-default" id="btnBuscar">
                <span class="glyphicon glyphicon-search"></span>Buscar
            </button>
        </div>
        <div id="divFactorActividadEconomica"></div>
    </div>
    <div class="back">

    </div>
</div>

@section Scripts{
    <script src="~/Scripts/Controls/chosen.jquery.js"></script>
    <script src="~/Scripts/conec/catalogo/factorActividadEconomica.js"></script>
}

