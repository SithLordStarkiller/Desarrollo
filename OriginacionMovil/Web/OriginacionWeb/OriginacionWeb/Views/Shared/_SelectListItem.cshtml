@model IEnumerable<SelectListItem>
@{
    var id = (string)ViewBag.IdDropDown;
}

@if (Model != null)
{
    @Html.DropDownList(id, Model, new { @class = "form-control", DataTextField = "Descripcion", CssClass = "Combos FormularioList" })
}