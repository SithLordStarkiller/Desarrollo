@model IEnumerable<ExamenCsi.Site.Models.UsUsuarioModel>

@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contrasena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contrasena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUsuarion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUsuarion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUsuarion })
        </td>
    </tr>
}

</table>
